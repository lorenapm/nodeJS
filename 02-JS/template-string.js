const name = "Deadpool";
const real = "Wade Winston";

const normal = name + " " + real;
const template = `${name} ${real}`;

console.log(normal);
console.log(template);

const html = `
<h1>Hola</h1>
<p>Mundo</p>`;

console.log(html);
